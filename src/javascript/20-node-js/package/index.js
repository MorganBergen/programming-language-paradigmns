function greet(name) {
    console.log(`hello ${name} welcome to node.js`);    
}

module.exports = greet;

/**
 * 
 * code to package and publish to the npm registry
 * but in order to do that, we need to create a package.json file
 */